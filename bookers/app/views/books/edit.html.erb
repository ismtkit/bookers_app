<h1>Editing</h1>
<%= form_with model:@book,  local:true do |f| %>
 <% if @book.errors.any? %>
 <h3><%= @book.errors.count %>error prohibited this book from being saved: </h3>
 <url>
  <% @book.errors.full_messages.each do |message| %>
  <%= message %>
  <% end %>
 </url>
<% end %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
<% end %>
<div>
  <span><%= link_to 'Show', "/books/#{@book.id}" %></span> |  <span><%= link_to 'Back', "/books" %></span>
</div>